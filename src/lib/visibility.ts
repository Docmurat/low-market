/**
 * ЕДИНОЕ витринное правило показа товара (решение вехи «модерация фото»):
 * товар виден в каталоге/поиске/подборках, только если у него есть фото
 * ИЛИ модератор явно опубликовал его с заглушкой (photoPlaceholder).
 *
 * Подмешивается через AND во все витринные запросы:
 * главная, каталог, поиск, кросс-продажи. Прямая ссылка на карточку
 * (/product/[slug]) работает всегда — чтобы не ломать корзины и старые ссылки.
 * Админка и /moder видят всё без этого фильтра.
 */
import type { Prisma } from '@prisma/client';

export const photoVisibleWhere: Prisma.ProductWhereInput = {
  OR: [{ images: { isEmpty: false } }, { photoPlaceholder: true }],
};
