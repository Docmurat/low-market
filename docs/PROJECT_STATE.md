# PROJECT_STATE — LOW-Market
Обновлено: 2026-08-30 (веха: МОДЕРАЦИЯ ФОТО — панель /moder, роль moderator, разбор
пачками со свайпом, чистка битых ссылок, зеркалирование ~34 тыс. фото в наш Object
Storage, витринное правило показа, заглушки; + домен low-market.ru и DNS готовы).
Файл живёт в репозитории: docs/PROJECT_STATE.md. Обновляется на каждой вехе.

## 1. Что за проект
Интернет-магазин техники LOW-Market (экс-«ВОЛЬТ»). Розница Москва/МО, компьютерная
техника — ядро ассортимента. Субдистрибьютор, кроссдокинг со склада поставщика
АБСОЛЮТ ТРЕЙД (группа ELKO), ~10 400 SKU через их API. ИП УСН.
Разработка: владелец + Claude, Windows/VSCode/GitHub/Yandex Cloud.

## 2. Дорожная карта и статус
| Шаг | Содержание | Статус |
|-----|-----------|--------|
| 1 | Каркас Next.js, БД, каталог, карточка, пайплайн синка | ✅ |
| 1.5 | Ребрендинг ВОЛЬТ → LOW-Market | ✅ |
| 6a | Доступ к API поставщика: прокси на сервере, токен, клиент, проба | ✅ |
| 6b | Реальный синк: дерево категорий → Category, 10 384 товара, цены/остатки, характеристики, фото; журнал SyncLog | ✅ |
| 2 | Фильтры по характеристикам, поиск, пагинация, сортировка, карточка товара (галерея, РРЦ, описание) | ✅ |
| 3 | Корзина (Cart/CartItem) + чекаут в 2 шага + Order/OrderItem + кросс-продажи | ✅ |
| 4 | Авторизация (телефон/email), ЛК: мои заказы, профиль, userId в Cart и Order, слияние корзин | ✅ |
| 5 | Админка: роль admin, заказы и статусы, маржа, экран SyncLog, перекладка категорий с защитой от синка | ✅ |
| МС-0 | Аккаунт МойСклад заведён и базово настроен (см. п.3) | ✅ |
| 6c | НОВАЯ РЕДАКЦИЯ: заказы поставщику — ВРУЧНУЮ в кабинете АБСОЛЮТ ТРЕЙД (решение владельца, API-заказов Shipment/CreateOrder НЕ делаем). В админке — панель помощи: список позиций с артикулами, «Скопировать список», живая проверка остатков/закупки, номер ручного заказа + напоминание | ✅ |
| 7 | ЮKassa: оплата сразу в чекауте, чеки 54-ФЗ, повторная оплата, webhook. ТЕСТОВЫЙ КОНТУР (боевые ключи, анкета ИП и webhook — на шаге 12) | ✅ |
| 7b | Живая проверка наличия у поставщика перед оплатой (fail-open, 6 с) + свежая закупка в снимок маржи | ✅ |
| МС | Интеграция «МойСклад» (заказы → МС, счета/УПД/ЭДО там). Перед стартом: реквизиты ИП в МС + проверить доступность API на тарифе (после триала аккаунт падает на «Бесплатный», на нём API может не быть) | ⬜ |
| 6d | Расписание синка на сервере (cron: prices каждые 30–60 мин — уточняем лимиты у поставщика, full ночью; + check-images и mirror-images после full) | ⬜ на шаге 12 |
| 8 | Доставка: зоны Москва/МО × обычный/крупногабарит, бесплатно от порога И ≤20% маржи, самовывоз бесплатно; доставка в чеке ЮKassa отдельной строкой-услугой. МОДЕЛЬ ВРЕМЕННАЯ — цифры и формула в src/lib/delivery/{config,calc}.ts | ✅ |
| 8b | Адресные подсказки DaData: поле с автодополнением в чекауте, зона по официальному региону подсказки, блок курьера вне Москвы/МО (пустой DADATA_API_KEY = форма полностью ручная) | ✅ |
| 9 | Отзывы: только купившие товар (paid/shipped/done по userId ИЛИ телефону), оценка 1–5 + текст, ОДИН отзыв без права правки (решение владельца), постмодерация (админ скрывает), блок покупок в ЛК с формой на месте (группы «Последние» без отзыва / «Прошлые» с отзывом). Бонус за отзыв — ОТЛОЖЕН (добавим потом, схема готова к промокодам) | ✅ |
| МФ | МОДЕРАЦИЯ ФОТО (веха 2026-08-30): хранилище Object Storage, роль moderator, панель /moder (без фото/заглушки/разбор), чистка 404, зеркалирование всех фото к нам, витринное правило показа. Подробно — п.4 «Модерация фото» и п.5 | ✅ |
| Д | ДЕПЛОЙ low-market.ru (веха 3, СЛЕДУЮЩАЯ): перенос БД и сайта на сервер, nginx с служебным паролем, https. Домен и DNS уже готовы. План — п.7 | ⬜ ТЕКУЩАЯ |
| 10 | B2B: оптовый заказ, счёт | ⬜ |
| 11 | PWA | ⬜ |
| 12 | Достройка прода: CI/CD, бэкапы БД, ужесточить SG, сменить SUPPLIER_PROXY_KEY, cron синка (6d) + cron чистки/зеркалирования фото, ВКЛЮЧИТЬ Telegram-алерты, БОЕВАЯ ЮKassa (анкета ИП, боевые ключи, SITE_URL=домен, webhook в кабинете), частый автосинк. Часть прежнего шага 12 (systemd, nginx) закроется вехой Д | ⬜ |
| Ц | ЦЕНООБРАЗОВАНИЕ (отложено): матрица по категориям, потолок РРЦ, НДС (+ vat_code в чеках ЮKassa) — см. п.5 | ⬜ отдельная задача |

## 3. Инфраструктура (актуально)
- GitHub: private-репозиторий https://github.com/Docmurat/low-market. Коммиты через
  Source Control в VSCode. .env в git не попадает.
- ЛОКАЛЬНО: Windows. Node v24, npm 11. Проект в C:\dev\voltshop.
  PostgreSQL 17 порт 5433 (!), база voltshop. На 5432 — чужой PostgreSQL 14, не трогать.
  Prisma 5.22 (баннер про 8.0 — игнорировать, не обновлять). Docker НЕ используется.
  Новые пакеты (веха МФ): sharp (обработка картинок), @aws-sdk/client-s3 (Object Storage).
- СЕРВЕР: Yandex Cloud, ВМ voltshop-prod (Ubuntu 24.04, 2 vCPU/4ГБ), IP 89.169.138.130.
  SSH: login ubuntu, ключ %USERPROFILE%\.ssh\voltshop, порты 22 и 443.
  ⚠ ДЛЯ ВЕХИ Д: порт 443 сейчас занят SSH — освободить под nginx/https ПЕРВЫМ шагом,
  не потеряв доступ (сначала проверить, что 22 доступен из текущей сети).
  Установлено: Node 20, PostgreSQL 16, git, nginx. БД на сервере не создана (веха Д).
  Группа безопасности Any/Any (ужесточить на шаге 12).
  VPN у пользователя нужен для Claude; для SSH/scp, скриптов синка, МойСклад и ЮKassa —
  ВЫКЛЮЧАТЬ (российские сервисы через VPN глючат).
  Из-за VPN-нестабильности Telegram с ПК работает через раз → алерты только с сервера (шаг 12).
- ДОМЕН/DNS (готово 2026-08-30): домен low-market.ru на nic.ru (оплачен до 26.08.2027).
  DNS-зона в Yandex Cloud DNS (публичная): A @ → 89.169.138.130, A www → тот же IP.
  На nic.ru NS переключены на ns1.yandexcloud.net / ns2.yandexcloud.net.
  ns1 отвечает правильно; мировое делегирование на момент записи доезжало (до суток).
  Проверка: nslookup low-market.ru → 89.169.138.130.
- OBJECT STORAGE (веха МФ): бакет low-market-images (чтение объектов публичное, листинг
  закрыт), сервисный аккаунт low-market-uploader (роль storage.uploader), статические
  ключи в .env: S3_ENDPOINT=https://storage.yandexcloud.net, S3_REGION=ru-central1,
  S3_BUCKET=low-market-images, S3_ACCESS_KEY_ID, S3_SECRET_ACCESS_KEY.
  Занято ~631 МБ (~34 тыс. webp) ≈ 15–20 ₽/мес. Витрина фото от поставщика НЕ зависит.
- ПРОКСИ К ПОСТАВЩИКУ (на сервере): nginx :8443, самоподписанный сертификат
  (копия в репо ops/nginx/supplier.crt), заголовок X-Proxy-Key = SUPPLIER_PROXY_KEY.
  NODE_EXTRA_CA_CERTS теперь прошит через cross-env в npm-скриптах (dev, sync, sync:prices,
  sync:specs, probe) — руками выставлять не нужно; нужен только для ручных `npx tsx …`.
- МОЙСКЛАД: аккаунт заведён 2026-08-28 на владельца (владелец сам ведёт учёт, отдельного
  бухгалтера НЕТ). Склад один — «Кроссдокинг (склад поставщика)». Юрлицо — заглушка,
  РЕКВИЗИТЫ ИП НЕ ВНЕСЕНЫ (хвост). Товары в МС вручную НЕ заводить — поедут через API
  при задаче «МС». Триал 14 дней (до ~2026-09-11), затем тариф «Бесплатный» (1 юрлицо);
  перед задачей «МС» проверить, есть ли API на тарифе, при необходимости — минимальный платный.
- ЮKASSA: аккаунт заведён 2026-08-28 по спец-ссылке https://yookassa.ru/joinups?createTestShop=true
  (короткий путь без юрданных; обычная регистрация требует ИНН). Работаем с ТЕСТОВЫМ
  магазином: YOOKASSA_SHOP_ID и YOOKASSA_SECRET_KEY (test_…) в .env. Договор не подписан,
  анкета ИП не заполнена — на шаге 12 (данные ИП заказчика пока не трогаем).
  Тестовая карта: 5555 5555 5555 4477, срок любой будущий, CVC любой.
  Самозанятость для приёма платежей НЕ вариант: НПД запрещает перепродажу товаров.
- DADATA (адресные подсказки, задача 8b): аккаунт на владельце, ключ в .env
  (DADATA_API_KEY; пусто = подсказки выключены, чекаут полностью ручной). Бесплатный
  тариф: 10 000 запросов/день (~10–30 на один ввод адреса, у нас дебаунс 300 мс);
  превышение/неподтверждённая почта = их 403 → у нас тихо пустой список. Ключ живёт
  ТОЛЬКО на сервере — браузер ходит в наш /api/address-suggest. В запросе
  locations_boost kladr 77/50 (Москва/МО в топ, другие регионы не скрыты). НА БУДУЩЕЕ:
  на платных тарифах DaData отдаёт beltway_distance (км от МКАД) — пригодится для зон
  по удалённости; «Город в службе доставки» (id СДЭК/Boxberry) — если самовывоз через ПВЗ.
- ПОСТАВЩИК — АБСОЛЮТ ТРЕЙД, eCommerce API v3. Токен в .env (SUPPLIER_API_TOKEN, 1 год).
  НЕ вызывать CreateToken. Поддержка: api@absoluttrade.ru. Пауза между запросами 3,2 с.
  Факты по данным (проверено на реальном фиде):
  - CategoryTree: 395 узлов (без мусорных веток), 294 активных; коды категорий НЕ уникальны
    (CBL, NIC, MAS, SPE, CAU…) → категории зеркалим по id узла, товар кладём по строке catalogTree.
  - У части узлов code=null (Умные колонки, Кабели питания…) — их товары через API не достать.
    РЕШЕНО 2026-08-30: эти товары в базу и так не попадают (синк обходит только листья с
    кодами) — просто не продаём, вопрос из письма поставщику убран.
  - ProductSearch по категории отдаёт всё одним ответом (пагинации нет); реально ~4% меньше,
    чем totalProducts в дереве.
  - inStock.quantity — строка: "0", "7", "> 10", "> 40", "> 100" → stock (число) + stockLabel.
  - eanCodes "-" = пусто. rrp может быть 0/пусто. У Lenovo и др. productPrice == rrp.
  - Description: 49 характеристик у ноутбука; ключ "Description" = HTML-текст описания
    (переносим в product.description), "Image" — дубль фото. У ~11% товаров specs пусты.
  - MediaItems: ThumbPicture + Picture; ссылки на selstorage.ru часто 404, относительные
    пути /upload/… → домен https://ecom.absoluttrade.ru (src/lib/supplier/media.ts).
    РЕШЕНО вехой МФ: мёртвые вычищены (391), живые зеркалированы к нам (см. п.4).
  - Покрытие характеристиками неровное: видеокарты/процессоры 85–95%, ноутбуки ~33%.
  - Пустые/почти пустые категории: Наушники (0 в наличии), Чехлы для смартфонов (1).
  Письмо поставщику: текст ГОТОВ в финальной редакции 2026-08-30 (два вопроса: лимиты
  частоты запросов для автосинка 30–60 мин; битые фото / выгрузка архивом). ОТПРАВИТЬ —
  за владельцем. Вопрос про НДС убран (закрыт), про code=null убран (решено), про
  тестовые API-заказы исключён (заказы вручную, 6c).
  ЗАКРЫТО 2026-08-29 (подтвердил владелец): ЦЕНЫ ПОСТАВЩИКА — С НДС. Конфиг уже верный
  (SUPPLIER_PRICES_INCLUDE_VAT=true), менять ничего не нужно.

## 4. Код (ключевые файлы)
### Каталог и синк
- prisma/schema.prisma — Category, Product (+ поля фида, categoryLocked; веха МФ:
  imagesLocked, photoReviewStatus, photoPlaceholder), ProductAttribute,
  SyncLog, Cart, CartItem, Order, OrderItem, User (role), Session, LoginCode, Review.
  Order дополнен: supplierOrderNumber/supplierOrderedAt (6c), paymentId/paymentStatus/paidAt
  (+ @@index([paymentId])) (шаг 7).
  Миграции: …_supplier_sync, …_cart, …_orders, …_users, …_admin_role_category_lock,
  …_supplier_manual_order, …_yookassa_payment, …_reviews, …_photo_moderation,
  …_photo_placeholder.
- scripts/sync-supplier.ts — синк: режимы full | prices | specs; --category=CODE, --limit=N,
  --no-specs, --refresh-specs. Полный: ~18 мин товары + ~1 ч характеристики. Идемпотентен.
  В конце full/specs вызывает buildAttributes. Пишет SyncLog.
  ВАЖНО: товары с categoryLocked=true — категория НЕ перезаписывается (карта lockedBySku
  в upsertProducts); цена/остаток обновляются как обычно.
  ВАЖНО (веха МФ): товары с imagesLocked=true — images НЕ трогаются ВООБЩЕ, даже пустые
  (модератор мог сознательно удалить весь мусор).
- scripts/build-attributes.ts — specs → ProductAttribute по конфигу фильтров + перенос
  Description поставщика в product.description (если пусто).
- scripts/reprice.ts — пересчёт цен по базе без API. scripts/fix-images.ts — починка фото.
- scripts/db-stats.ts, scripts/spec-stats.ts <slug>, scripts/probe-supplier.ts.
- scripts/make-admin.ts <телефон> [--revoke] — назначение/снятие роли admin.
- scripts/sync-alert.ts — алерт в Telegram по последнему SyncLog (error / errors>0 /
  висит running > 3 ч); --test — проверка настройки. ВКЛЮЧАЕМ НА ШАГЕ 12 (с ПК Telegram
  нестабилен из-за VPN). sync-supplier.ts им НЕ трогается — запускается следом отдельно.
- src/lib/pricing.ts — FLAT_MARKUP_PCT из .env (сейчас 10) побеждает всё; матрица и floor
  остаются в коде для будущего; НДС-переключатель не активен.
- src/lib/telegram.ts — sendTelegram/isTelegramConfigured (TELEGRAM_BOT_TOKEN,
  TELEGRAM_CHAT_ID в .env; пусто = выключено). Без 'server-only' — используется скриптами.
- src/lib/supplier/{absolut.ts, category-rules.ts, media.ts}.
- src/lib/filters/{config.ts, normalize.ts}; src/lib/catalog/query.ts (фильтры, фасеты,
  сортировка, пагинация 48, поиск).
- src/app/catalog/[slug]/page.tsx, src/app/search/page.tsx, src/app/product/[slug]/page.tsx;
  src/components/catalog/{FilterSidebar, SortBar, Pagination, ProductGrid}; ProductCard
  (принимает id + gism → показывает кнопку «В корзину»; веха МФ: без image рисует
  заглушку /photo-stub.svg); src/components/product/Gallery.tsx.
  Веха МФ: каталог/поиск подмешивают photoVisibleWhere в base-условие (AND), карточка
  товара при пустых images отдаёт в Gallery ['/photo-stub.svg'].
### Модерация фото (веха МФ, 2026-08-30)
- src/lib/visibility.ts — photoVisibleWhere: ЕДИНОЕ витринное правило — товар без фото
  и без заглушки НЕ показывается (каталог, поиск, главная, кросс-продажи). Прямая
  ссылка /product/[slug] работает всегда. Вшито: src/app/page.tsx,
  catalog/[slug]/page.tsx, search/page.tsx, src/lib/crosssell/query.ts.
- public/photo-stub.svg — фирменная заглушка «Фото скоро появится».
- src/lib/storage.ts — клиент Object Storage: isStorageConfigured, uploadToStorage,
  deleteFromStorageByUrl (чужие ссылки молча пропускает), publicStorageUrl.
  Без 'server-only' — используется скриптами.
- src/lib/image-process.ts — processProductImage: EXIF-поворот → белый фон → вписать
  в 1200×1200 (fit inside, МЕЛКОЕ НЕ РАСТЯГИВАЕМ — иначе мыло) → webp q82.
- src/lib/staff.ts — requireModerator(): пускает admin и moderator; не вошёл →
  /account/login, без прав → молча на главную. auth.ts не тронут.
- src/lib/auth-shared.ts — UserRole = customer | admin | moderator; canModerate().
- src/lib/moder-categories.ts — категории для панели: loadModerCategories, catLabel,
  subtreeIds (выбранная категория + ВСЕ подкатегории — «умный» фильтр).
- src/app/moder/layout.tsx — requireModerator на каждый заход; вкладки «Без фото» /
  «Разбор фото». /moder живёт ОТДЕЛЬНО от /admin (модератору заказы/маржа не видны).
- src/app/moder/photos/page.tsx — вкладки со счётчиками: «Скрыты с витрины» (без фото,
  без заглушки) и «Заглушки»; поиск, умный фильтр категорий, пагинация 30, ходовые первыми.
- src/app/moder/photos/[id]/page.tsx — статус витрины (Виден / С заглушкой / Скрыт),
  кнопки «Опубликовать с заглушкой»/«Снять», текущие фото с «Убрать», форма добавления.
- src/app/moder/photos/actions.ts — uploadPhoto (НЕСКОЛЬКО файлов разом ИЛИ ссылка;
  ссылка может вести на страницу товара — сервер достаёт og:image/twitter:image;
  маркетплейсы сознательно не поддерживаются; загрузка снимает photoPlaceholder и
  ставит imagesLocked), removePhoto (ставит imagesLocked; наши файлы чистятся из
  бакета), togglePlaceholder.
- src/components/moder/PhotoUploadForm.tsx — файлы (multiple) или ссылка; подсказка
  «источник — официальный сайт производителя»; автоочистка после успеха.
- src/app/moder/review/page.tsx — разбор пачками: целые товары на ~50 фото
  (BATCH_PHOTOS=50, предохранитель 40 товаров); очереди основная/«Отложенные»
  (?queue=skipped); умный фильтр категории держится через пачки; ходовые первыми.
- src/components/moder/ReviewDeck.tsx — «тиндер»: ← плохое, → хорошее, ↓ пропустить
  товар, Backspace/«← Назад» — откат СКВОЗЬ границы товаров; на телефоне СВАЙП
  влево/вправо (анимация, подсказки «Плохое»/«Хорошее», порог 80px, вертикальный
  скролл не ломается). НИЧЕГО не удаляется по ходу: экран подтверждения — все
  «плохие» миниатюрами по товарам, «↩ Вернуть» у каждого, метка «останется без фото»,
  затем «Сохранить пачку». «Завершить пачку сейчас» — досрочно (недосмотренный товар
  остаётся в очереди). Решения до сохранения живут в браузере.
- src/app/moder/review/actions.ts — applyReviewBatch(items, skippedIds): одно action
  на пачку; keep пересекается с реальными images; ставит imagesLocked и
  photoReviewStatus='done' (skipped — 'skipped'); наши файлы удаляются из бакета.
- scripts/load-env.ts — общий загрузчик .env для скриптов (ПЕРВЫМ импортом:
  import './load-env'); npx tsx сам .env не читает, dotenv в проекте нет.
- scripts/make-moderator.ts <телефон> [--revoke] — роль moderator (человек сначала
  сам входит на сайт по телефону); админа этим скриптом не трогает.
- scripts/check-images.ts [--dry] [--limit=N] — чистка ссылок: удаляет ТОЛЬКО 404/410,
  сомнительные (таймауты, прочие коды) не трогает; кэш повторяющихся ссылок.
  ВЫПОЛНЕНО: 8 692 товара / 32 594 ссылок, удалена 391, 13 товаров остались без фото.
- scripts/mirror-images.ts [--limit=N] — зеркалирование: скачать → обработать → в бакет
  → подменить ссылку; наши ссылки пропускает (перезапускаемый), 404/410 выбрасывает,
  временные сбои оставляет на следующий прогон; ходовые первыми.
  ВЫПОЛНЕНО: ~34 000 фото, ~631 МБ; осталось 7 упрямых чужих ссылок (не блокер).
- scripts/list-mirrored.ts — шпаргалка «какие товары уже на наших фото» со ссылками.
- next.config.mjs — experimental.serverActions.bodySizeLimit='10mb' (иначе фото
  с телефона не пролезает в server action).
- ПРАВИЛО ЭКСПЛУАТАЦИИ: после full/--refresh-specs синка новым товарам приходят
  ссылки поставщика → перегонять check-images и mirror-images (на шаге 12 — в cron).
### Корзина и заказы (Шаг 3)
- src/lib/cart-shared.ts — константы/типы/проверки БЕЗ серверных импортов (можно в клиенте):
  CART_COOKIE=lm_cart (90 дней), MAX_QTY=99, checkPurchasable, clampQty, cartTotals.
- src/lib/cart.ts — серверное чтение: getCart(), getCartCount() (для шапки). Импортирует
  next/headers — В КЛИЕНТСКИЕ КОМПОНЕНТЫ НЕ ИМПОРТИРОВАТЬ.
- src/app/cart/actions.ts — server actions: addToCart, setItemQty, removeItem, clearCart.
  Все проверки на сервере; после изменений revalidatePath('/', 'layout').
- src/app/cart/page.tsx; src/components/cart/{AddToCartButton, CartItemRow} (client).
- src/lib/checkout-shared.ts — типы CheckoutData, parseCheckout (валидация), normalizePhone
  (→ +7XXXXXXXXXX), formatPhone, formatAddress, DELIVERY_OPTIONS (courier | pickup),
  ORDER_STATUS_LABEL.
- src/app/checkout/actions.ts — saveCheckout (шаг 1 → cookie lm_checkout, сутки),
  placeOrder (шаги 7/7b/8): проверка по БД → расчёт доставки calcDelivery (ДО живой
  проверки, по данным БД — цифра обязана совпасть с показанной на подтверждении) →
  ЖИВАЯ проверка остатков у поставщика (AvailabilityAndPrice, таймаут 6 с, fail-open →
  работаем по БД; свежая закупка пишется в снимок basePrice) → транзакция Order +
  OrderItem → платёж ЮKassa (доставка в чеке отдельной строкой-услугой) → redirect на
  платёжную форму (или на страницу заказа, если оплата выключена/не создалась).
### Доставка (Шаг 8)
- src/lib/delivery/config.ts — ВСЕ ЦИФРЫ (временные): тарифы msk/mo × regular/bulky
  (500/1500, 900/2500), FREE_DELIVERY_FROM_RUB=10000, FREE_DELIVERY_MAX_MARGIN_SHARE=0.2
  (решение владельца: бесплатно только если доставка ≤ 20% маржи заказа; при плоской
  +10% реально включается от ~27 500 ₽ — осознанно), BULKY_CATEGORY_PATTERNS (regex по
  названию категории: мониторы, моноблоки, МФУ, принтеры, корпуса, ИБП, телевизоры).
- src/lib/delivery/calc.ts — calcDelivery() (чистая, без серверных импортов): зона —
  СНАЧАЛА по официальному региону из подсказки DaData (regionToZone: 'г Москва' → msk,
  'Московская обл' → mo, прочее → null = курьером не возим), запасной вариант — по
  полю «город» (detectZone: /москва/i → msk, иначе mo), крупногабарит по категории,
  правило бесплатной доставки (курьер + не КГТ + порог + доля маржи).
  Зовут её ДВОЕ на одних данных БД: confirm-страница (показ) и placeOrder (заказ/чек).
- Снимок в Order.deliveryCost; страница заказа и чеки показывают Товары/Доставка/Итого.
### Адресные подсказки (8b)
- src/app/api/address-suggest/route.ts — серверная прослойка к DaData Suggest
  (ключ не светится в браузере; любой сбой/лимит = пустой список, форма живёт).
- src/components/checkout/AddressSuggest.tsx — поле «начните вводить» (дебаунс 300 мс,
  мин. 3 символа; при пустом ключе прячет себя после пробного запроса на маунте).
- CheckoutForm: город/улица/дом — управляемые (подсказка их заполняет), region —
  скрытое поле; ручная смена ГОРОДА сбрасывает region. CheckoutData.region живёт
  только в cookie чекаута, в Order НЕ пишется (миграции не было — сознательно).
- checkout-shared.parseCheckout: region известен и не Москва/МО → ошибка «курьером
  только Москва и МО — выберите самовывоз»; ручной ввод (region пуст) не блокируем.
- src/app/checkout/page.tsx (шаг 1; предзаполняется из профиля, черновик из cookie
  приоритетнее), src/app/checkout/confirm/page.tsx (шаг 2).
- src/components/checkout/{CheckoutForm (useFormState), PlaceOrderButton («Подтвердить
  и оплатить»)}.
### Оплата ЮKassa (Шаг 7)
- src/lib/payment/yookassa.ts — клиент API: isPaymentEnabled (пустые ключи = выключено),
  createOrderPayment (одностадийный, capture:true, чек 54-ФЗ, идемпотентность), getPayment,
  siteUrl (SITE_URL из .env). RECEIPT_VAT_CODE=6 («без НДС») — поменять при задаче «Ц».
- src/app/order/[token]/page.tsx — страница заказа: syncPayment при открытии (сама
  спрашивает ЮKassa и переводит new → paid; замена webhook-а локально, подстраховка на
  проде), баннеры «остался шаг — оплата» / «принят» (без оплаты) / «оплачен», кнопка оплаты.
- src/app/order/[token]/actions.ts — payForOrder: повторный платёж со страницы заказа
  (доступ по accessToken, каждая попытка = новый платёж).
- src/components/order/PayOrderButton.tsx.
- src/app/api/yookassa/webhook/route.ts — webhook: телу не доверяем, статус перепроверяем
  через API. Локально не работает (localhost недостижим для ЮKassa) — включим на проде.
### Авторизация и ЛК (Шаг 4)
- src/lib/auth-shared.ts — константы/проверки БЕЗ серверных импортов: SESSION_COOKIE=lm_session
  (30 дней), CODE_LENGTH=6, CODE_TTL_MIN=5, CODE_MAX_ATTEMPTS=5, CODE_RESEND_SEC=60,
  PASSWORD_MIN_LENGTH=8, isValidEmail, passwordProblem, isValidCodeFormat, UserRole
  (customer | admin | moderator), canModerate, тип UserView (с role).
- src/lib/auth.ts — серверное ядро (next/headers — в клиент НЕ импортировать):
  getSessionUser(), requireAdmin() (не вошёл → /account/login, не админ → /),
  createSession(), destroySession(), hashPassword/verifyPassword (node:crypto scrypt,
  "scrypt:соль:хеш"), generateLoginCode (randomInt).
- src/lib/sms/index.ts — sendLoginCode; SMS_PROVIDER в .env пуст = локальный режим,
  код печатается в терминал dev-сервера. Провайдер НЕ выбран (ПОДВЕШЕНО, решение
  владельца 2026-08-30; критично для прода — без SMS вход только у тех, кто уже в БД).
- src/lib/cart-merge.ts — mergeGuestCartIntoUser: гостевая корзина привязывается или
  доливается в корзину пользователя (qty с потолком остатка/MAX_QTY); чужая корзина
  в cookie не сливается. Ставит cookie → звать только из server actions.
- src/app/account/login/actions.ts — requestCode / verifyCode / loginWithPassword.
  verifyCode при первом входе создаёт User (upsert по phone) = регистрация.
- src/app/account/login/page.tsx; src/components/account/LoginForm.tsx (client, вкладки
  «По телефону» и «Email и пароль», двухшаговый ввод кода).
- src/app/account/page.tsx — ЛК: профиль-карточка + «Мои заказы» (where userId OR phone —
  гостевые заказы подтягиваются); ссылки ведут на /order/<accessToken>.
- src/app/account/actions.ts — logout (удаляет сессию И cookie корзины).
- src/app/account/profile/{page.tsx, actions.ts}; src/components/account/ProfileForm.tsx —
  имя/email (P2002 → «email занят»), установка/смена пароля (требует email).
- src/components/Header.tsx — имя пользователя (или «Кабинет») вместо «Войти»;
  жёлтая «Админка» для admin; «Модерация» для admin И moderator (веха МФ).
### Админка (Шаг 5 + 6c)
- src/app/admin/layout.tsx — requireAdmin() на каждый заход + левое меню
  (Сводка / Заказы / Синхронизация / Товары / Отзывы).
- src/app/admin/page.tsx — сводка: заказы по статусам с суммами, последние 5,
  каталог (всего/активных/в наличии/gism, пользователи), последний SyncLog.
- src/app/admin/orders/page.tsx — список: вкладки по статусам, страницы по 30.
- src/app/admin/orders/[id]/page.tsx — карточка: смена статуса кнопками, ПАНЕЛЬ «Заказ
  у поставщика (вручную)» (6c), покупатель/адрес, состав с закупкой, маржой по позициям
  и итогом (₽ и %); ссылка на страницу покупателя.
- src/app/admin/orders/actions.ts — setOrderStatus (requireAdmin внутри action).
- src/app/admin/orders/supplier-actions.ts — checkSupplierAvailability (живые остатки/
  закупка, только чтение, один запрос на все позиции), saveSupplierOrderNumber (номер
  ручного заказа; отметка времени при первом сохранении; пустой номер = сброс).
- src/components/admin/SupplierOrderPanel.tsx — таблица позиций, «Скопировать список»,
  «Проверить у поставщика», форма номера; жёлтое напоминание, пока номер не вписан
  (гаснет также при cancelled/done).
- src/app/admin/sync/page.tsx — журнал 50 прогонов; src/app/admin/sync/[id]/page.tsx —
  счётчики + хвост консольного лога.
- src/app/admin/products/page.tsx — поиск по артикулу/названию/бренду, фильтр «только
  с ручной категорией»; src/app/admin/products/[id]/page.tsx — select всех активных
  категорий (label = supplierPath), «Сохранить и защитить от синка», снятие защиты.
- src/app/admin/products/actions.ts — setProductCategory (categoryLocked=true),
  unlockProductCategory.
### Отзывы (Шаг 9)
- prisma: модель Review (rating 1–5, text, authorName-снимок, isHidden,
  @@unique(productId,userId)); миграция …_reviews; связи reviews в Product и User.
- src/app/reviews/actions.ts — submitReview: авторизация → проверка покупки
  (orderItem в заказе paid/shipped/done по userId ИЛИ phone) → ТОЛЬКО create
  (existing → отказ «изменить нельзя»; catch на двойной клик). Ревалидирует
  карточку товара и /account.
- src/components/product/Reviews.tsx (server) — блок на карточке: средняя оценка,
  список видимых (не isHidden), форма только купившим БЕЗ своего отзыва; свой
  отзыв показывается строкой «Ваш отзыв: ★…». Экспортирует Stars.
- src/components/product/ReviewForm.tsx (client) — звёзды + текст; без режима правки.
- src/app/admin/reviews/{page,actions}.tsx — раздел «Отзывы» в админке: вкладки
  Все/Скрытые, кнопка Скрыть/Показать (toggleReviewHidden, requireAdmin).
- src/components/account/{PurchasedProducts (server, данные), PurchasedList (client)} —
  покупки в ЛК: «Последние покупки» (без отзыва, форма раскрывается НА МЕСТЕ),
  «Прошлые покупки» (с отзывом, свёрнуты, только звёзды). Блок сам прячется без покупок.
### Кросс-продажи
- src/lib/crosssell/config.ts — 16 правил «категория-источник → категории-цели» по НАЗВАНИЮ
  категории (точные regex с $, чтобы не подмешивать серверные/Enterprise ветки).
- src/lib/crosssell/query.ts — getCrossSell: accessories (по правилу, в наличии, без gism,
  перемешивание) + similar (та же категория, цена ±40%). Кэш дерева категорий 5 мин.
  Веха МФ: оба подбора учитывают photoVisibleWhere.
- src/components/product/CrossSell.tsx — блоки «С этим товаром покупают» и «Похожие товары».
- scripts/crosssell-check.ts — проверка правил по реальным названиям категорий.
- Запуск скриптов: `npx tsx scripts/<имя>.ts`; npm-скрипты: sync, sync:prices, sync:specs,
  probe, seed. В dev/sync*/probe cross-env подставляет NODE_EXTRA_CA_CERTS автоматически.
- .env (кроме DATABASE_URL и секретов): FLAT_MARKUP_PCT=10, SUPPLIER_PRICES_INCLUDE_VAT=true,
  VAT_PCT=22, SMS_PROVIDER= (пусто = локально), TELEGRAM_BOT_TOKEN= и TELEGRAM_CHAT_ID=
  (пусто = алерты выключены до шага 12), YOOKASSA_SHOP_ID= и YOOKASSA_SECRET_KEY=
  (сейчас тестовые; пусто = оплата выключена), SITE_URL=http://localhost:3000 (на проде —
  https://low-market.ru), S3_ENDPOINT/S3_REGION/S3_BUCKET/S3_ACCESS_KEY_ID/
  S3_SECRET_ACCESS_KEY (Object Storage, веха МФ).

## 5. Ключевые решения (не пересматривать без причины)
- Кастомный стек вместо Битрикс.
- ЦЕНЫ СЕЙЧАС: плоская наценка 10% (FLAT_MARKUP_PCT) поверх цены поставщика как есть, без НДС,
  без потолка РРЦ. Причина: схема расчётов (нал/безнал, НДС по категориям) не прояснена.
  ОТЛОЖЕНО, НЕ ЗАБЫТЬ: матрица по категориям (код готов), потолок РРЦ (у 3918 товаров наша
  цена выше РРЦ), НДС (+ RECEIPT_VAT_CODE в чеках ЮKassa, сейчас 6 = «без НДС»).
  Возврат к этому — отдельная задача «Ц» после ответа поставщика/бухгалтера.
- ОПЛАТА — СРАЗУ В ЧЕКАУТЕ (решение владельца, 2026-08-28): покупатель платит при
  оформлении, если всё в наличии; заказ приходит к нам УЖЕ ОПЛАЧЕННЫМ, дальше быстро
  заказываем у поставщика и оформляем доставку. Прежняя схема «оплата после подтверждения
  менеджером» ОТМЕНЕНА. Статус confirmed остаётся как ручной (B2B, особые случаи).
  new = создан-не-оплачен (на странице заказа — кнопка «Оплатить», попытки не ограничены).
- ДОСТАВКА (Шаг 8, модель ВРЕМЕННАЯ — менять можно только src/lib/delivery/): зона по
  городу × габаритный класс по категории; бесплатно ТОЛЬКО курьером, без КГТ, от порога
  И если тариф ≤ 20% маржи заказа (защита маржи — решение владельца). КГТ бесплатным не
  бывает. Самовывоз бесплатный, «место и время согласует менеджер» (своей точки нет).
  Стоимость — снимком в Order.deliveryCost; в чеке ЮKassa — строка «Доставка» (услуга),
  чек обязан сходиться с платежом. ORDER_STATUS_LABEL: new = «ожидает оплаты».
- ЗАКАЗЫ ПОСТАВЩИКУ — ВРУЧНУЮ (решение владельца, 2026-08-28, отменяет старый план 6c):
  Shipment/CreateOrder и любые пишущие вызовы к API поставщика НЕ используем. Панель в
  карточке заказа админки помогает: список, живая проверка, номер ручного заказа
  (supplierOrderNumber) + автоотметка времени; напоминание горит, пока номер пуст.
- Наличие при оплате — два рубежа (7b): БД (последний синк) + живой запрос поставщику
  с fail-open (API молчит → чекаут работает по БД, не падает). На проде третьей опорой
  станет частый автосинк (шаг 12).
- МойСклад: владелец сам ведёт учёт, отдельного бухгалтера нет. Аккаунт МС — на владельце.
- Боевой приём платежей — ТОЛЬКО на ИП. Самозанятость (НПД) не подходит: перепродажа
  товаров на НПД запрещена. Тестовый контур ЮKassa юрлица не требует.
- Категории = зеркало дерева поставщика (3 уровня), ключ supplierId; slug по имени.
  Мусорные ветки (zzz…, Рекламные материалы, Услуги, Комплекты) не берём.
- Артикул товара = productId поставщика. Slug товара = бренд-название-productId.
- Защищённые поля: description и images не затираются; если пусто — заполняем от поставщика.
  specs всегда перезаписываются из Description; ProductAttribute перестраивается.
  categoryLocked=true (переложен вручную в админке) → синк категорию НЕ перезаписывает.
  imagesLocked=true (веха МФ: фото правил модератор) → синк images НЕ трогает ВООБЩЕ.
- Пропавшие из фида и isEol → деактивация. Категории без товаров → isActive=false.
- gism=true → флаг «Честный ЗНАК» (442 товара): в корзину НЕ кладём (кнопка «Скоро в продаже»),
  из кросс-продаж исключены. Откроем после ЭДО + кассы с маркировкой.
- Корзина: в БД, id в httpOnly-cookie lm_cart; userId null = гостевая. Цены в корзине
  не фиксируются (всегда актуальные), снимок — только в OrderItem. Кол-во ≤ остатка и ≤ 99.
  В снимок basePrice при живой проверке пишется закупка на момент оплаты (честная маржа).
- Заказ: статус строкой (new → confirmed → paid → shipped → done | cancelled), без enum —
  чтобы менять без миграций. Номер LM-000123. Страница заказа — по accessToken (cuid).
  Адрес — структурой (city/street/house/apartment/entrance/floor/intercom) под зоны доставки.
  deliveryCost=0 до шага 8 («рассчитаем при подтверждении»).
- АВТОРИЗАЦИЯ (Шаг 4): регистрация ТОЛЬКО по телефону — аккаунт создаётся автоматически
  при первом входе по SMS-коду (User.phone обязателен/уникален, на нём держится подтягивание
  гостевых заказов). Email+пароль — ВТОРОЙ способ входа, задаётся в профиле. Отдельной
  регистрации по email нет — сознательно, чтобы не плодить аккаунты без телефона.
- Сессии в БД (Session, cookie lm_session, 30 дней), пароли — node:crypto scrypt.
  Сторонних auth-библиотек нет. SMS-код: 6 цифр, 5 минут, 5 попыток, повтор раз в минуту.
- При выходе удаляется и cookie корзины (общие компьютеры). «Мои заказы» = userId OR phone.
- ОТЗЫВЫ (Шаг 9): подтверждённая покупка обязательна; отзыв публикуется ОДИН раз,
  правок нет (решение владельца — никаких надписей об этом пользователю не показываем,
  повторная отправка просто получает отказ). Постмодерация: виден сразу, админ скрывает.
  Бонус за отзыв отложен. В ЛК форма отзыва живёт прямо в списке покупок (без переходов).
- АДМИНКА (Шаг 5): роль строкой в User (customer | admin | moderator, без enum),
  назначение — scripts/make-admin.ts и scripts/make-moderator.ts. /admin закрыт
  requireAdmin() в layout И в каждом server action. Отдельного логина админки нет —
  вход обычный, аккаунт помечен ролью. Не-админа уводим на главную молча.
- МОДЕРАЦИЯ ФОТО (веха МФ, решения владельца 2026-08-30):
  - Товар БЕЗ фото на витрине НЕ показывается — пока модератор не загрузит фото или
    не опубликует с заглушкой (photoPlaceholder + /photo-stub.svg). Прямая ссылка
    на карточку живёт всегда (корзины, старые ссылки не ломаются).
  - Роль moderator видит ТОЛЬКО /moder (панель фото); заказы/маржа/админка закрыты.
  - Источник фото — ОФИЦИАЛЬНЫЕ сайты производителей (файлом, ссылкой на картинку
    или ссылкой на страницу — og:image). Маркетплейсы/Я.Картинки — НЕЛЬЗЯ и не
    поддерживаются: капчи, чужие фото, авторские права. Парсер маркетплейсов НЕ делаем.
  - Все фото витрины живут в НАШЕМ Object Storage (зеркалирование выполнено);
    от серверов поставщика показ не зависит. Обработка едина: ≤1200×1200 webp,
    мелкое не растягиваем.
  - Разбор «тиндером» пачками ~50 фото: по ходу НИЧЕГО не удаляется, удаление только
    после экрана подтверждения с «Вернуть» (защита от ошибочного свайпа).
  - Разобранный товар (done) в очередь разбора НЕ возвращается, даже после смены фото.
  - Приоритет работы модератора — через фильтр категории (с подкатегориями), начиная
    с маржинальных (ноутбуки и т.п.) — отдельной механики приоритетов нет, не нужна.
- ДЕПЛОЙ (решения владельца 2026-08-30, веха Д): база с ПК — источник правды, переезжает
  дампом ОДИН раз; после снятия дампа работа с ДАННЫМИ на ПК прекращается навсегда
  (сервер = данные, ПК = код и песочница; между базами данные НЕ синхронизируются,
  ездит только код через git + миграции структуры). Синки переводим на сервер.
  До готовности сайт закрыт СЛУЖЕБНЫМ ПАРОЛЕМ (nginx basic auth) + запрет индексации;
  оплата пока остаётся тестовой (боевая ЮKassa — шаг 12).
- РАЗДЕЛЕНИЕ КОНТУРОВ: админка сайта = операционный контур (заказ, оплата, маржа,
  категории витрины, журнал синка, ручной заказ поставщику). МойСклад = учётный контур
  (счета, УПД, ЭДО, Честный ЗНАК). Заказы поедут в МС при интеграции (задача «МС»);
  статусы для покупателя живут на сайте. Дублирования нет.
- Telegram-алерты: код готов (telegram.ts + sync-alert.ts), НО включаем только на шаге 12
  на сервере — с ПК из-за VPN доставка нестабильна, алертам «через раз» доверять нельзя.
- Фильтры: состояние в URL (?brand=MSI&vram=16+ГБ&instock=1&sort=price_asc) — ссылки для рекламы.
- Учёт: на старте «МойСклад» (счета, УПД, ЭДО, Честный ЗНАК, API), не 1С.
- Все обращения к API поставщика — с IP сервера (локально через прокси).
- Бренд: LOW-Market. CSS-токен цвета `volt` в Tailwind НЕ переименовывать.
- Домен low-market.ru: куплен на nic.ru, DNS ведём в Yandex Cloud DNS (NS у Яндекса).

## 6. Известные грабли
- Порт 22 к серверу недоступен из части сетей → запасной 443. НО на вехе Д 443 уходит
  под https — перед освобождением убедиться, что 22 из текущей сети работает.
- Prisma в песочнице Claude не работает — типы проверяются заглушкой/esbuild.
- `npx prisma migrate dev` на Windows — ТОЛЬКО при остановленном dev-сервере (иначе
  EPERM на движке). После миграции VSCode подсвечивает новые поля красным →
  TypeScript: Restart TS Server (это не ошибка кода, а неперечитанные типы клиента).
- Краснота на импортах из 'react-dom' (useFormState) = не хватает @types/react-dom →
  npm install -D @types/react-dom@18 (поставлен).
- Prisma Studio: фильтр isNull не работает — считать через scripts/db-stats.ts.
- Многострочные вставки рвутся в терминале → команды по одной.
- Закрытый терминал = потерянный вывод; для долгих прогонов: `… *> data\имя.log`;
  следить из ВТОРОГО терминала: Get-Content data\имя.log -Wait -Tail 10 (Ctrl+C
  останавливает только просмотр). Терминал с работающим скриптом НЕ закрывать;
  на долгие прогоны отключить сон ПК.
- Переменные $env:… живут только в своём терминале и умирают при перезапуске — из-за этого
  «Проверить у поставщика» падала с fetch failed после рестарта dev-сервера. РЕШЕНО:
  cross-env в npm-скриптах; для ручных `npx tsx …` переменная всё ещё нужна.
- npx tsx НЕ читает .env (dotenv в проекте НЕТ — Next читает .env сам) → в скриптах
  ПЕРВЫМ импортом `import './load-env'` (scripts/load-env.ts, веха МФ). В разовых
  `npx tsx -e` скриптах require('dotenv') упадёт; проверять .env через fs.readFileSync.
- redirect() Next-а работает через исключение → НЕЛЬЗЯ звать внутри try/catch (улетит в
  catch). Паттерн: в try только собираем URL, redirect один — после try/catch.
- Webhook ЮKassa до localhost не достаёт → локально статус оплаты подтягивает страница
  заказа (syncPayment) при открытии; webhook включаем на проде и оставляем syncPayment
  подстраховкой.
- Обычная регистрация ЮKassa требует юрданные без вариантов → тестовый магазин заводится
  по ссылке https://yookassa.ru/joinups?createTestShop=true («перейти к тестированию»).
- Тестовый платёж проходит ТОЛЬКО тестовой картой ЮKassa; обычная карта и настоящий
  кошелёк дают ошибку — это норма.
- Sticky-панель фильтров длиннее экрана не прокручивается — убрали sticky.
- Неконтролируемые чекбоксы не сбрасываются при смене URL → checked={…} + key на форме.
- Модуль с `import { cookies } from 'next/headers'` нельзя импортировать из 'use client'
  компонента (даже ради типа/константы) → общее выносим в *-shared.ts.
- cookies().set() работает только в server action / route handler, не в page.tsx.
- Из файла с 'use server' можно экспортировать ТОЛЬКО async-функции и типы (константы
  нельзя) → начальные состояния useFormState живут в клиентском компоненте.
- Длинные .tsx с вложенными шаблонными строками ломаются при копировании из чата →
  отдавать файлами на скачивание / в zip, синтаксис перед выдачей проверять esbuild-ом.
- useFormState с двумя формами (запрос кода / проверка кода): у формы проверки начальное
  состояние должно быть phase:'code', иначе шаг ввода кода никогда не покажется.
- В сводке админки ссылки «Последние заказы» ведут на страницу ПОКУПАТЕЛЯ (/order/token);
  кнопки смены статуса — только в /admin/orders/[id]. Не путать при проверках.
- Правки в большие существующие файлы: просить пользователя прикрепить файл в чат,
  править точечно, возвращать целиком (поиск по проекту даёт лишь фрагменты).
  PROJECT_STATE при обновлении — ТОЛЬКО дополнять на основе прежней версии, не писать
  с нуля по памяти чата (2026-08-30 чуть не потеряли разделы — поймал владелец).
- Битые фото поставщика (404 на selstorage) — РЕШЕНО вехой МФ: чистка + зеркалирование;
  правило на будущее — перегонять оба скрипта после full/--refresh-specs синка.
- dev-сервер таймаутит на selstorage/чужих картинках при включённом VPN — выключить VPN;
  после зеркалирования проблема ушла (фото свои).
- Секрет прокси, токен, ключи ЮKassa/DaData/S3 и .env в чат не вставлять. В git не
  коммитить .env и data\*.log.
- В имени переменной .env может прятаться БУКВА-ДВОЙНИК (кириллическая А и т.п.) —
  глазами не видно, Node переменную «не находит». Лечение: удалить строку и набрать
  имя РУКАМИ в английской раскладке. Диагностика: печать codePointAt по символам имени.
- PowerShell показывает русский текст из JSON-ответов и логов кракозябрами — это
  косметика консоли, НЕ баг: в браузере/файле всё нормально.
- МойСклад, ЮKassa, Telegram, SSH, DaData, консоль Yandex Cloud, selstorage — при
  включённом VPN глючат: выключать.
- nic.ru: у домена изначально включена «Статусная страница» (NS statuspage1/2.nic.ru) —
  своей DNS-зоны нет; мы увели NS в Yandex Cloud DNS. Смена NS доезжает до суток;
  проверка напрямую: nslookup low-market.ru ns1.yandexcloud.net.

## 7. Следующий шаг (для нового чата) — ВЕХА Д: деплой low-market.ru
0) Проверить: mirror-images и чистка выполнены (да), веха МФ закоммичена (да),
   nslookup low-market.ru отвечает 89.169.138.130 (если ещё нет — работать по IP,
   https-сертификат сделать последним шагом).
1) ПОРЯДОК ДЕПЛОЯ (решения в п.5 «ДЕПЛОЙ»):
   а) освободить порт 443 от SSH (оставить 22), не потеряв доступ;
   б) на сервере: проверить Node 20/PostgreSQL 16 (стоят), создать БД voltshop и
      пользователя;
   в) дамп локальной БД (pg_dump с ПК, порт 5433!) → перенос на сервер (scp) →
      восстановление; С ЭТОГО МОМЕНТА данные — только на сервере;
   г) git clone репозитория на сервер; боевой .env (DATABASE_URL сервера, S3_*,
      SUPPLIER_* — прокси теперь локальный для сервера, ЮKassa тестовые ключи,
      DADATA_API_KEY, SITE_URL=https://low-market.ru); npm ci;
      npx prisma migrate deploy; npm run build;
   д) systemd-служба Next (порт 3000, автозапуск, рестарт при падении);
   е) nginx: reverse proxy → 3000, БАЗОВАЯ АУТЕНТИФИКАЦИЯ (служебный пароль),
      client_max_body_size ≥ 10m (загрузка фото модератором!), заголовок/robots
      noindex;
   ж) certbot / Let's Encrypt на low-market.ru и www (когда DNS доехал);
   з) проверки: вход, каталог (фото из бакета), заказ с тестовой оплатой,
      /moder с телефона (свайп), панель заказов;
   и) модератор: человек регистрируется на боевом сайте по телефону →
      make-moderator.ts НА СЕРВЕРЕ; сообщить ему служебный пароль nginx.
      ВНИМАНИЕ: SMS-провайдера нет → SMS-код на проде НЕ уходит. Для входа
      модератора: временно смотреть код в логах сервера (journalctl) ИЛИ
      ускорить выбор SMS-провайдера (хвост). Решить с владельцем по месту.
   к) обновить этот файл (сайт на сервере) + закоммитить.
2) После вехи Д — счёт вех 3/3: обновить PROJECT_STATE и перейти в новый чат (регламент).
3) Затем шаг 10 — B2B (оптовый заказ, счёт по форме для юрлиц); перед кодом
   обсудить с владельцем: реквизиты в счёте, кто выставляет (сайт или МойСклад).
4) Хвосты без кода (в любой момент): ОТПРАВИТЬ письмо api@absoluttrade.ru (текст
   готов, редакция 2026-08-30: лимиты + битые фото/архив); внести реквизиты ИП в
   МойСклад; ВЫБРАТЬ SMS-провайдера (подвешено; критично для прода);
   регистрация в Честном ЗНАКе и ЭДО.
5) На шаге 12 не забыть (накопилось): боевая ЮKassa (анкета ИП, боевые ключи,
   webhook + события payment.succeeded/payment.canceled в кабинете), частый автосинк
   (30–60 мин по лимитам поставщика) + check-images/mirror-images в cron после full,
   Telegram-алерты, смена SUPPLIER_PROXY_KEY, ужесточить SG, бэкапы БД, CI/CD.
6) Перед задачей «МС»: реквизиты ИП внесены, тариф МС выбран с доступом к API.
7) Задача «Ц» частично разблокирована: цены поставщика С НДС (подтверждено) — при
   возврате к «Ц» пересмотреть матрицу наценок, потолок РРЦ и RECEIPT_VAT_CODE в чеках.
   НЕ трогать без команды владельца.