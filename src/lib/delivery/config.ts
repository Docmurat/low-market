/**
 * Конфиг доставки (шаг 8). ВСЕ ЦИФРЫ ВРЕМЕННЫЕ — заменить после расчёта реальной
 * логистики. Менять можно только этот файл: формула живёт в calc.ts, остальной код
 * (чекаут, чек ЮKassa, страницы) зовёт calcDelivery() и о цифрах не знает.
 * Без серверных импортов — можно использовать где угодно.
 */

/** Тарифы, ₽: зона × габаритный класс. */
export const DELIVERY_TARIFFS_RUB = {
  msk: { regular: 500, bulky: 1500 }, // Москва
  mo: { regular: 900, bulky: 2500 }, // Московская область (всё, что не Москва)
} as const;

/** Бесплатная доставка: сумма заказа от … ₽ (первое из двух условий). */
export const FREE_DELIVERY_FROM_RUB = 10_000;

/**
 * …И тариф доставки не дороже этой доли маржи заказа (второе условие, решение
 * владельца: доставка бесплатна, только если она ≤ 20% маржи). При плоской
 * наценке +10% правило реально включается от ~27 500 ₽ — это осознанная защита
 * маржи; после задачи «Ц» (матрица наценок) порог станет мягче сам собой.
 */
export const FREE_DELIVERY_MAX_MARGIN_SHARE = 0.2;

/** Крупногабарит определяется по НАЗВАНИЮ категории товара. Бесплатным не бывает. */
export const BULKY_CATEGORY_PATTERNS: RegExp[] = [
  /монитор/i,
  /моноблок/i,
  /телевизор/i,
  /мфу/i,
  /принтер/i,
  /корпус/i,
  /ибп|бесперебойн/i,
];

/** Самовывоз: бесплатно; место и время согласует менеджер (своей точки пока нет). */
export const PICKUP_IS_FREE = true;
